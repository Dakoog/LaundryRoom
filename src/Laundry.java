public class Laundry {
    // szablon
    /*
    classes - laundry, customer,order,  payment, order item
    enum - service,  assortment orderStatus (placed,ready,realised) delivery (self-pickup , home delivery)
           payment type (card, cash, blik)

    laundry  methods: place order , deliver , markReady, pay
     List array list (orders)
     map hashmap (number, customer)

     customer
     fields: name, address

     order
     field customer
     field status
     field boolean (paid)
     list array list (order item)
     methods: markReady, markDelivered, summarize, markPaid

 order item
 fields: service, assortment, price, amount



     */
}
